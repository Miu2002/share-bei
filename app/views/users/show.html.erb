<div class="main user-show">
  <div class="container">
  <main>
  <div class="wrapper">
  <h1 class="sec-title">My page</h1>
  <section id="news" class="wrapper">
  <table style="width :60%;">
  <tr><td style="text-align :center;">
      Name：<%= @user.name %></td></tr>
      <td style="text-align :center;">E-mail：<%= @user.email  %></td>
      <% if @user.id == @current_user.id %>
      <tr><td style="text-align :center;"><button>
      <%= link_to("プロフィールを編集","/users/#{@user.id}/edit") %></button></td></tr>
      </table></section>
      <section id="news" class="wrapper">
    <h3 class="sec-title">過去の投稿</h3>
    <table><tr>
    <% @post.each do |post| %>
    <td style= "margin :0 20px 0 0;"><%= post.shop.name %></td>
    <td><%= truncate(post.content, length: 15) %></td>
    <td><button class= "edit">
    <%= link_to "編集",edit_shop_post_path(post_id:post.id, shop_id:@shop.id) %></button></td>
      </tr>
     <% end %>
     </table>
    <p id="button">    
    <%= link_to("/favorites/index") do%>
      <button>My検討リスト</button>
       </p>
      <% end %>
      <% end %>
    </div>
  </div>
  