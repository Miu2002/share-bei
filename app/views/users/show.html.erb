<div class="main user-show">
  <div class="container">
  <main>
  <div class="wrapper">
  <h1 class="sec-title">My page</h1>
  <section id="news" class="wrapper">
      <h2><%= @user.name %><br>
      メールアドレス:<%= @user.email %></h2>
      <% if @user.id == @current_user.id %>
      <button><%= link_to("編集","/users/#{@user.id}/edit") %></button>
      <section id="news" class="wrapper">
    <h3 class="sec-title">過去の投稿</h3>
    <div>
    <% @post.each do |post| %>
    <p>□<%= post.shop.name %>
    <%= post.content %>
    <button class= "edit">
    <%= link_to "編集",edit_shop_post_path(post_id:post.id, shop_id:@shop.id) %></button><br></p>
         <% end %>
        </div> 
    <button><%= link_to'my検討リスト',{controller: 'posts', action: 'show'},{method: :get} %></button>
      <% end %>
    </div>
  </div>
 